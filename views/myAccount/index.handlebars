<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">Example</h5>
          <p>
            <strong>ID</strong>: {{users.id}}</p>
          <p>
          <p>
            <strong>Text</strong>: {{users.user_name}}</p>
          <p>
            <a href="/">← Back To Home</a>
            <a href="/logout">Logout</a>
        </div>
      </div>
    </div>
  </div>
</div>
